services:
  smallholder:
    build: .
    ports:
      - "8080:80"
    volumes:
      - smallholder-data:/app/data
    environment:
      - DB_PATH=/app/data/smallholder.db

volumes:
  smallholder-data:
